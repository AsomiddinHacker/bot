# Telepilot API

Production-ready backend foundation for Telegram AI business bot (Telepilot).

## Quick start

1. Copy `.env.example` to `.env`.
2. Install dependencies: `npm install`
3. Run development mode: `npm run dev`
4. Check health endpoint: `GET http://localhost:3000/api/v1/health`
